# <Job name>

A starting skeleton for the file you point `--skill` at. The prompt stays a
pointer; everything that defines the work lives here, where it can be edited and
version-controlled without touching the scheduler.

## Goal

One sentence. What this job is for, stated so that a session can tell whether it
succeeded.

## Before doing anything

1. Read `progress.md` next to this file.
2. Score the previous session against what it predicted. Write the real numbers
   down before deciding what to do today — otherwise every session repeats the
   same plan regardless of whether it worked.

## Today's loop

1. …
2. …
3. …

## Hard limits

- Never do more than N of <the risky action> in one session.
- Stop all write operations immediately if <signal of trouble>.
- Never invent numbers or quotes. Missing data is reported as missing.

## State to update before finishing

- `progress.md`: what was done, what was measured, what is now pending.
- Anything under "Needs a human" that came up during the session.

## Last step

Overwrite the summary file named in the prompt with a short report: what was
done, the real numbers, what failed, and what is waiting on a human. Keep the
section order stable so consecutive reports can be compared at a glance.
